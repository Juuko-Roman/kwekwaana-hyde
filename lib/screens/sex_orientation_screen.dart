import 'package:flutter/material.dart';

class sexOrientationScreen extends StatefulWidget {
  const sexOrientationScreen({Key? key}) : super(key: key);

  @override
  State<sexOrientationScreen> createState() => _sexOrientationScreenState();
}

class _sexOrientationScreenState extends State<sexOrientationScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
